name: fungit_app
description: "Fungit - identificación de setas + chat"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.6.18
  provider: ^6.0.5
  http: ^0.13.5
  image_picker: ^0.8.7+4
  path_provider: ^2.0.12
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_fungit.png